---
import type { Lang } from "../data/landing";

interface Props {
  title: string;
  description: string;
  lang: Lang;
}

const { title, description, lang } = Astro.props;
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/logo.svg" />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#080808" />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content="/logo.svg" />
    <link rel="alternate" hreflang="en" href={`${Astro.url.origin}/`} />
    <link rel="alternate" hreflang="es" href={`${Astro.url.origin}/es/`} />
    <link rel="alternate" hreflang="pt" href={`${Astro.url.origin}/pt/`} />
    <link rel="alternate" hreflang="fr" href={`${Astro.url.origin}/fr/`} />
    <link rel="alternate" hreflang="de" href={`${Astro.url.origin}/de/`} />
    <link rel="alternate" hreflang="it" href={`${Astro.url.origin}/it/`} />
    <link rel="alternate" hreflang="x-default" href={`${Astro.url.origin}/`} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
    <title>{title}</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  :root {
    color-scheme: dark;
    font-family: Inter, system-ui, -apple-system, sans-serif;
    font-size: 16px;
    line-height: 1.6;

    /* Brand tokens */
    --brand-primary: #ff8c00;
    --brand-primary-hover: #ff9f2e;
    --brand-primary-active: #e67e00;
    --brand-primary-subtle: rgba(255, 140, 0, 0.08);

    /* Surfaces */
    --color-bg: #080808;
    --color-bg-gradient:
      radial-gradient(ellipse 90% 60% at 50% -10%, rgba(255, 140, 0, 0.07) 0%, transparent 60%),
      #080808;
    --color-surface: #111111;
    --color-surface-elevated: #181818;
    --color-glass-bg: rgba(255, 255, 255, 0.04);
    --color-glass-border: rgba(255, 255, 255, 0.08);
    --color-border: rgba(255, 255, 255, 0.1);

    /* Text */
    --color-text: #f0f0f0;
    --color-text-muted: #888888;
    --color-text-subtle: #555555;

    /* Semantic (roadmap / status) */
    --semantic-success: #22c55e;
    --semantic-warning: #f5a623;
    --semantic-info: var(--brand-primary);

    /* Shape */
    --radius: 12px;
    --radius-sm: 8px;
    --radius-lg: 20px;
    --blur: 20px;
    --transition: 200ms ease-out;
  }

  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    margin: 0;
    min-height: 100vh;
    color: var(--color-text);
    background: var(--color-bg-gradient);
    background-attachment: fixed;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  a {
    color: inherit;
  }

  img,
  svg {
    display: block;
  }

  /* ── Shared layout primitives (used across all pages) ── */
  .page {
    max-width: 1160px;
    margin: 0 auto;
    padding: 0 24px;
  }

  /* ── Buttons ── */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 12px 26px;
    font-size: 0.9375rem;
    font-weight: 600;
    font-family: inherit;
    border-radius: var(--radius-sm);
    text-decoration: none;
    transition: background var(--transition), color var(--transition),
      border-color var(--transition), transform var(--transition);
    cursor: pointer;
  }

  .btn--primary {
    background: var(--brand-primary);
    color: #0d0d0d;
    border: 1px solid transparent;
  }

  .btn--primary:hover {
    background: var(--brand-primary-hover);
    transform: translateY(-1px);
  }

  .btn--ghost {
    background: transparent;
    color: var(--color-text);
    border: 1px solid rgba(255, 255, 255, 0.16);
  }

  .btn--ghost:hover {
    background: rgba(255, 255, 255, 0.06);
    border-color: rgba(255, 255, 255, 0.3);
    transform: translateY(-1px);
  }

  /* ── Eyebrow pill ── */
  .eyebrow {
    display: inline-block;
    padding: 6px 14px;
    font-size: 0.8125rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    color: var(--brand-primary);
    background: var(--brand-primary-subtle);
    border: 1px solid rgba(255, 140, 0, 0.22);
    border-radius: 100px;
  }

  /* ── Section heading block ── */
  .section-head {
    text-align: center;
    margin-bottom: 44px;
  }

  .section-head__title {
    font-size: clamp(1.6rem, 3.2vw, 2.25rem);
    font-weight: 700;
    letter-spacing: -0.015em;
    margin: 0 0 12px;
    color: var(--color-text);
  }

  .section-head__subtitle {
    font-size: 1rem;
    color: var(--color-text-muted);
    margin: 0 auto;
    max-width: 54ch;
    line-height: 1.6;
  }

  /* ── Maturity status badge (shared across pages) ──
     Honest 3-state signal: live 🟢 / testing 🟡 / roadmap ⚪. */
  .status-badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 0.625rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    padding: 3px 9px;
    border-radius: 100px;
    color: var(--color-text-muted);
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    white-space: nowrap;
  }

  .status-badge::before {
    content: "";
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: currentColor;
    flex-shrink: 0;
  }

  .status-badge--live {
    color: var(--semantic-success);
    background: rgba(34, 197, 94, 0.12);
    border-color: rgba(34, 197, 94, 0.28);
  }

  .status-badge--testing {
    color: var(--semantic-warning);
    background: rgba(245, 166, 35, 0.12);
    border-color: rgba(245, 166, 35, 0.3);
  }

  .status-badge--roadmap {
    color: var(--color-text-subtle);
    background: rgba(255, 255, 255, 0.04);
    border-color: rgba(255, 255, 255, 0.1);
  }

  /* ── Charter section primitives (shared across charter sections) ── */
  .section {
    padding: 72px 0;
    position: relative;
  }

  .section--alt {
    background:
      radial-gradient(ellipse 80% 100% at 50% 0%, rgba(255, 140, 0, 0.04) 0%, transparent 70%),
      rgba(255, 255, 255, 0.014);
    border-top: 1px solid rgba(255, 255, 255, 0.05);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  }

  .sec-head {
    max-width: 60ch;
    margin: 0 0 40px;
  }

  .sec-head--center {
    margin-left: auto;
    margin-right: auto;
    text-align: center;
  }

  .sec-num {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    color: var(--color-text-subtle);
    font-variant-numeric: tabular-nums;
  }

  .sec-kicker {
    display: block;
    margin: 10px 0 12px;
    font-size: 0.8125rem;
    font-weight: 600;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--brand-primary);
  }

  .sec-title {
    font-size: clamp(1.55rem, 3.2vw, 2.3rem);
    font-weight: 700;
    letter-spacing: -0.02em;
    line-height: 1.15;
    margin: 0 0 14px;
    color: var(--color-text);
  }

  .sec-lead {
    font-size: 1.0625rem;
    color: var(--color-text-muted);
    line-height: 1.6;
    margin: 0;
  }

  .card {
    background: var(--color-surface);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: var(--radius-lg);
    padding: 26px;
  }

  .card__tag {
    display: inline-block;
    font-size: 0.6875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--brand-primary);
    background: var(--brand-primary-subtle);
    border: 1px solid rgba(255, 140, 0, 0.22);
    border-radius: 100px;
    padding: 4px 11px;
    margin-bottom: 14px;
  }

  .card__title {
    font-size: 1.125rem;
    font-weight: 700;
    letter-spacing: -0.01em;
    margin: 0 0 8px;
    color: var(--color-text);
  }

  .card__text {
    font-size: 0.9375rem;
    color: var(--color-text-muted);
    line-height: 1.6;
    margin: 0;
  }
</style>
